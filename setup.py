from setuptools import setup, find_packages

setup(
        name="trajprocess",
        version='4.1.0',
        packages=find_packages(),
        zip_safe=False,
        include_package_data=True,
)
